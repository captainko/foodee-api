version: '3'
services: 
  app: 
    container_name: docker-node-mongo
    restart: always
    build: .
    ports:
      - '8443:8443'
      - '80:80'
      - '443:443'
    network_mode: host
    networks:
      - foodee-net
  mongo:
    container_name: mongo
    image: mongo
    volumes: 
      - mongodata:/data/db
    networks:
      - foodee-net
volumes:
  mongodata:
    driver: local
networks:
  foodee-net:
