version: '3'
services: 
  app: 
    container_name: docker-node-mongo
    restart: always
    build: .
    ports:
      # - '8443:4500'
      - '8080:4500'
      - '80:80'
      - '443:443'
    networks:
      - foodee-net
  mongo:
    container_name: mongo
    image: mongo
    volumes: 
      - mongodata:/data/db
    networks:
      - foodee-net
volumes:
  mongodata:
    driver: local
networks:
  foodee-net:
